# Attacker LLM Agent Configuration

llm:
  model: "gpt-4o-mini"            # OpenAI backend model
  temperature: 0.7
  ollama_model: "deepseek-r1:70b"  # Ollama backend model

available_attacks:
  - sign_flip
  - noise_injection
  - scaling

memory:
  persist_path: "checkpoints/attacker_memory"
