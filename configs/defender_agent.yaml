# Defender LLM Agent Configuration

llm:
  model: "gpt-4o-mini"
  temperature: 0.3

initial_strategy:
  method: "norm_threshold"
  threshold: 2.0

memory:
  persist_path: "checkpoints/defender_memory"
